{
  "name": "flamo",
  "version": "0.1.0",
  "description": "Bootstrap the local Flamo MCP setup wizard",
  "license": "Apache-2.0",
  "bin": {
    "flamo": "bin/flamo.cjs"
  },
  "files": [
    "bin",
    "lib",
    "README.md"
  ],
  "scripts": {
    "test": "node --test",
    "lint": "oxlint",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check ."
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "oxc-transform": "^0.60.0",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.75.0"
  },
  "engines": {
    "node": ">=18"
  }
}
